# OWASP LLM02:2025 — age symmetric secret key. Springtale's own vault
# format uses XChaCha20-Poly1305, but age is common in user envs and
# any leak is critical.

id = "33_credential_age_secret"
description = "age (FiloSottile/age) secret key leaked"
category = "credential"
input_parts = [
    "The backup key is AGE-SECRET-",
    "KEY-1QRSABCDEF0123456789QRSABCDEF0123456789QRSABCDEF0123456789Q\n",
]
expected_pattern_type = "Credential"
expected_blocked = false

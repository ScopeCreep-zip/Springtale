# OWASP LLM02:2025 — AWS access key leaked alongside a secret-key
# variable assignment. The AKIA-prefix detector and the `secret=...`
# detector should both fire; the harness only asserts the AKIA hit.

id = "46_credential_aws_secret_pair"
description = "AWS access key id + secret leaked together"
category = "credential"
input_parts = [
    "AWS_ACCESS_KEY_ID=AKIAIOSFO",
    "DNN7EXAMPLE AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG",
    "/bPxRfiCYEXAMPLEKEY\n",
]
expected_pattern_type = "Credential"
expected_blocked = false

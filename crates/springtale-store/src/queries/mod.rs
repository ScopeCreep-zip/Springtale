// Query modules — SQL queries are currently inline in SqliteBackend.
// These modules will be populated if queries grow complex enough to
// warrant extraction. For now, all SQL lives in backend/sqlite.rs.
